import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(() => ({
    iconDown: {
        color: '#f50057',
        marginTop: '6px',
    },
    iconUp: {
        color: '#00AA00',
        marginTop: '6px',
    },
    iconNeutral: {
        color: '#000000',
        marginTop: '6px',
    },
    iconNone: {
        color: '#cccccc',
        marginTop: '6px',
    },
})
)

export default useStyles
const styles = theme => ({
    content: {
        margin: theme.spacing(1),
    },
    tablePaper: {
        marginTop: theme.spacing(2),
        padding: theme.spacing(2),
    }
})

export default styles
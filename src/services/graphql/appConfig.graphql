
query appConfiguredQuery  {
    appConfigured
}

mutation createRootUserMutation($secret:String!, $email:String!, $password:String!) {
    createRootUser(secret: $secret, email: $email, password: $password)
}
